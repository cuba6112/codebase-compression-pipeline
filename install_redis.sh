#!/bin/bash
pip3 install redis python-magic

echo "✅ Redis Python library installed!"
echo "To install system Redis: brew install redis && brew services start redis"
echo "To install Go: brew install go"